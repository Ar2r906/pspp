import { createRouter, createWebHistory } from 'vue-router'


const routes = [
  {
    path: '/',
    name: 'home',

  },
  {
    path: '/about',
    name: 'about',    
  }
]

const router = createRouter({
  
})

export default router
